# Raw responses

Bootstrap mode performs no network requests. The restored survey contains derived CSV evidence, not original HTTP response bodies, so no response has been synthesized here. Live mode stores response bodies in this directory.
